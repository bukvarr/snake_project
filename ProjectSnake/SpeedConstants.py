import Util
import GameConstants

forward_speed = Util.Point(0, -GameConstants.object_size)
backward_speed = Util.Point(0, GameConstants.object_size)
left_speed = Util.Point(-GameConstants.object_size, 0)
right_speed = Util.Point(GameConstants.object_size, 0)

speed_update_period = 2

